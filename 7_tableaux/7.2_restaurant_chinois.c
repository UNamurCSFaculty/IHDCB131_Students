#include <stdio.h>

int main(void) {
    int plat_le_plus_consome;

    // TODO: déclarer un tableau de 20 éléments initialisés à 0 (en une seule ligne)

    // TODO: faire une boucle qui demande des nombres entre 0 et 19,
    //       qui incrémente l'élément du tableau correspondant au nombre entré et
    //       qui s'arrête quand un nombre hors de cette plage est entré

    // TODO: trouver le plat le plus consommé

    printf("Le plat le plus consommé est le numéro %d\n", plat_le_plus_consome);

    return 0;
}