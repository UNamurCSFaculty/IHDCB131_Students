#include <stdio.h>

// TODO: définir la fonction "taille" qui calcule le nombre de caractères ('\0' non inclus) dans une chaine de caractères

int main(void) {
    int taille_mot;

    // TODO: récupérer un mot de l'utilisateur

    // TODO: appeler la fonction "taille" pour calculer la taille du mot

    printf("La taille du mot est : %d\n", taille_mot);

    return 0;
}